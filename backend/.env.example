# RECONX AI Server Configuration
PORT=5000
DATABASE_URL="file:./dev.db"

# AI Configuration (Optional: OpenAI key. If omitted, deterministic financial AI engine handles all queries)
OPENAI_API_KEY=
OPENAI_MODEL="gpt-4o-mini"

# Matching Defaults
DEFAULT_AMOUNT_TOLERANCE=0.01
DEFAULT_DATE_TOLERANCE_DAYS=3
DEFAULT_SENSITIVITY=balanced
